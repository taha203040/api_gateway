# services:
#   nginx : 
#     image: nginx:latest
#     container_name: nginx_gateway
#     ports:
#       - "80:80"
#     volumes:
#       - ./gateway/etc/nginx/nginx.config
#     depends_on :
#       - checkout
#       - user 
#     networks:
#       - api_gatewayN
#   user:
#     build: ./user
#     container_name: user_cont
#     volumes:
#       - ./user:/app
#       - /app/node_modules
#     ports:
#       - "3001:3001"
#     develop:
#       watch:
#         - path: ./user/package.json
#           action: rebuild
#         - path: ./user/package-lock.json
#           action: rebuild
#         - path: ./user/
#           target: /app
#           action: sync
#     restart: always
#     networks:
#       - api_gatewayN
#   checkout:
#     build: ./checkout
#     container_name: checkout_cont
#     volumes:
#       - ./checkout:/app
#       - /app/node_modules
#     ports:
#       - "3002:3000"
#     develop:
#     networks:
#       - api_gatewayN
# networks:
#   app-network:
#     driver: bridge
    
services:
  nginx:
    image: nginx:latest
    container_name: nginx_gateway
    ports:
      - "80:80"
    volumes:
      - ./gateway/etc/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - auth
      - checkout
    # develop:
    #   watch :
    #   - path : /gateway/etc/nginx/nginx.conf
    #     action: rebuild
      
    networks:
      - app-network
  user_db:
    image: postgres:alpine
    container_name: user_db
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: users_db
    ports:
      - "5432:5432"
    volumes:
      - users_data:/var/lib/postgresql/data
    networks:
      - app-network
      
  checkout_db:
    image: postgres:alpine
    container_name: checkout_db  
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: checkouts_db
    ports:
      - "5433:5433"  
    volumes:
      - checkout_data:/var/lib/postgresql/data  
    networks:
      - app-network

  auth:
    build: ./auth
    container_name: auth_cont
    volumes:
      - ./auth:/app
      - /app/node_modules
    ports:
      - "3001:3001"
    restart: always
    command: npm run dev
    networks:
      - app-network
    depends_on:
      - user_db
    develop:
      watch:
        - path: ./auth/package.json
          action: rebuild
        - path: ./auth/package-lock.json
          action: rebuild
        - path: ./auth/tsconfig.ts
          action : rebuild
        - path: ./auth/
          target: /app
          action: sync
  checkout:
    build: ./checkout
    container_name: checkout_cont
    volumes:
      - ./checkout:/app
      - /app/node_modules
    ports:
      - "3002:3002"
    restart: always
    command: npm run dev
    networks:
      - app-network
    depends_on:
      - checkout_db
    develop:  
      watch:
        - path: ./checkout/tsconfig.ts
          action : rebuild
        - path: ./checkout/package.json
          action: rebuild
        - path: ./checkout/package-lock.json
          action: rebuild
        - path: ./checkout/
          action: sync
          target: /app
networks:
  app-network:
    driver: bridge

volumes:
  users_data:
  checkout_data: